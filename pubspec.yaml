name: nrf_ble_dfu
description: Core Nordic BLE DFU protocol implementation in Flutter. Helps enable firmware updates of BLE devices over Bluetooth Low Energy.
version: 0.1.0
repository: https://github.com/chan150/nrf_ble_dfu
homepage: https://github.com/chan150/nrf_ble_dfu

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.7.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus:
  flutter_blue_plus_winrt:
  archive:
  path:
  intl:

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0
