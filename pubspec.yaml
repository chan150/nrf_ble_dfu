name: nrf_ble_dfu
description: Core Nordic BLE DFU protocol in pure Dart. Transport-agnostic; supply a DfuDevice.
version: 2.0.0
repository: https://github.com/chan150/nrf_ble_dfu
homepage: https://github.com/chan150/nrf_ble_dfu

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  archive:
  path:
  intl:

dev_dependencies:
  test: ^1.24.0
