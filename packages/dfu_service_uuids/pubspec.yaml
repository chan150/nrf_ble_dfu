name: dfu_service_uuids
description: The service UUIDs identifying the Nordic DFU and MCUmgr SMP protocols, shared by both families.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  test: ^1.24.0
